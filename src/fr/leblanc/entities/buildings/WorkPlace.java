package fr.leblanc.entities.buildings;

public class WorkPlace extends Construction{
    public WorkPlace(int width, int length, boolean protectionNeeded) {
        super(width, length, protectionNeeded);
    }
}
